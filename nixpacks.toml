# Railway Nixpacks configuration for monorepo
# This file tells Railway how to build the Next.js app

[phases.build]
cmds = [
    "cd apps/web && npm install && npm run build"
]

[start]
cmd = "cd apps/web && npm start"
