[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm ci --omit=dev"
]

[phases.build]
cmds = [
  "echo 'No build step required for legacy runtime'"
]

[start]
cmd = "node src/app.js"
