[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm install --omit=dev --no-audit --no-fund"
]

[phases.build]
cmds = [
  "echo 'No build step required for legacy runtime'"
]

[start]
cmd = "node src/app.js"
